


function tipValue(){
  //first we select the bill amount from html//
  let amount = document.getElementById('bill1').value;
  /*Next we declare the constant variable tip as 0.2, in order to represent the tip percent in this case 20%*/
const tipPercent=0.2;
let tip=(amount*(tipPercent));
        alert(tip)
  //lastly we calculate the tip//
}

 